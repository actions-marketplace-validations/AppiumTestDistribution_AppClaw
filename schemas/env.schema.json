{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AppClaw Environment",
  "description": "Environment file for AppClaw variable/secret bindings (.appclaw/env/<name>.yaml)",
  "type": "object",
  "properties": {
    "secrets": {
      "type": "object",
      "description": "Sensitive values — referenced as ${secrets.KEY} in flows. Values can be literals or ${ENV_VAR} references resolved from .env files.",
      "additionalProperties": {
        "type": "string",
        "description": "Secret value or ${ENV_VAR} reference"
      },
      "examples": [
        {
          "email": "${TEST_USER_EMAIL}",
          "password": "${TEST_USER_PASSWORD}",
          "api_key": "${API_KEY}"
        }
      ]
    },
    "variables": {
      "type": "object",
      "description": "Non-sensitive configuration — referenced as ${variables.KEY} in flows. Values are plain literals.",
      "additionalProperties": {
        "oneOf": [{ "type": "string" }, { "type": "number" }, { "type": "boolean" }],
        "description": "Variable value (string, number, or boolean)"
      },
      "examples": [
        {
          "base_url": "https://dev.myapp.com",
          "locale": "en-US",
          "timeout": 30,
          "debug": true
        }
      ]
    }
  },
  "additionalProperties": false
}
